server:
  port: 8083

spring:
  application:
    name: transaction-service
  
  datasource:
    url: jdbc:h2:mem:transaction_db
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

# JWT Configuration
jwt:
  secret: bank-microservices-shared-jwt-secret-2024
  expiration: 86400000

# Other service URLs
services:
  customer:
    url: http://localhost:8081

logging:
  level:
    com.bank.transaction: INFO